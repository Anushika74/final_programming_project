# Base URL for the backend API. In development the Vite proxy handles "/api",
# so this can stay empty. For production builds set it to your backend origin,
# e.g. https://systemiq.example.com
VITE_API_BASE_URL=
# WebSocket URL. Leave empty to auto-derive from the current page origin.
VITE_WS_URL=
